"""Serializers del contrato REST de `identity` (anillo externo).

Contendra los serializers de registro, login y perfil. Su responsabilidad es
exclusivamente sintactica: formato, tipos y campos requeridos. Las reglas de
negocio viven en `services.py` y en `domain/` (CLAUDE.md, regla 4).

Se implementa en M1.
"""
